{% set items = menus.main_menu.items %}

<header>
  <nav>
    <ul>
      {% for item in items %}
        <li><a href="{{ item.url }}">{{ item.name }}</a></li>
      {% endfor %}
    </ul>
  </nav>
</header>
